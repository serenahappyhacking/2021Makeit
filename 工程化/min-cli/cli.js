/**
 * commander， 对命令行解析，获取命令行中的参数，知道用户输入了什么命令
 * inquirer， 命令行中的表单
 * chalk， 命令行中的可变颜色效果
 * clui， 命令行中的 Loading 效果
 * child_process，子进程模块，node 原生模块，提供一些方法让我们能够执行新的命令.
 * 比如child_process.exec(command[, options][, callback]) 用于新建一个异步子进程，然后缓存运行结果，运行结束后调用callback;
 * 同步子进程执行 child_process.execSync(command[, options])
 *
 *
 */
