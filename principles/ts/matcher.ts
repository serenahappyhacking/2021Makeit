export function createMatcher(
  routes: Array<RouteConfig>,
  router:Vuerouter
): Matcher {
  const { pathList, pathMap, nameMap }  = createRouteMap(routers)

  function addRoutes(routes) {
    
  }
}